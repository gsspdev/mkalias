# mkalias is a simple cl tool to simplify creating aliases in zsh

## To install simply plase the 'mkalias' executable somewhere in your $PATH i.e. ~/bin/mkalias

## The command has the format: "mkalias 'name' 'command'"
